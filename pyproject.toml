[project]
name = "pymadx"
dynamic = ["version"]
readme = "README.md"


[build-system]
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]

[tool.setuptools_scm]
write_to = "src/pymadx/_version.py"